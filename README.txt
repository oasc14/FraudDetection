####### Script For Fraud Detection #######
This script uses an unsupervised Machine Learning technique for detect anomalies in a dataset using MiniSom.
This a kind of an artifical neural network to represent a discrete samples, called a map.

In the figure we can view the map of a data set that used in the example.
 